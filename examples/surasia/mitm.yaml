domains:
  # blobs-infiniteugc.svc.halowaypoint.com
  blobs:
    - path: /ugcstorage/*.(jpg|png)
      methods:
        - GET
      response:
        body: https://cataas.com/cat
        headers:
          content-type: image/jpeg

  # gamecms-hacs.svc.halowaypoint.com
  gamecms:
    - path: /:title/images/file/news/*.(jpg|png)
      methods:
        - GET
      response:
        body: https://cataas.com/cat
        headers:
          content-type: image/jpeg
