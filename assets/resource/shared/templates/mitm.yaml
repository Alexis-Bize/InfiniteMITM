discovery: # discovery-infiniteugc.svc.halowaypoint.com
    - path: /hi/playlists/:guid/versions/:guid
      methods:
          - GET
      response:
          headers:
              x-infinite-mitm: :mitm-version
